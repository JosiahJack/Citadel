using UnityEngine;

namespace CSM.Editor {
    public class CachedShadowMenus : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
